import React from "react";
// import noteContext from "../context/notes/noteContext";

export default function About() {
//   const a = useContext(noteContext);
    console.log("something")

  return <div>biotch what about</div>;
}
